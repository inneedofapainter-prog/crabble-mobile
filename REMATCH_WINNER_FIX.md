# Rematch + Winner Screen Fix

Changes in this update:

- Fixed online rematch so the Rematch button starts a fresh multiplayer round after a finished game.
- Fixed the end screen so only the actual winner sees “You Win!”.
- Non-winning online players now see “You Lost” with the winner’s name.
- Server keeps lobby/player status in sync when a rematch starts.
- Existing Postgres leaderboard, win/loss, streaks, GI word fix, lobby, challenge, share app, dump rule, and GO rule updates are preserved.
